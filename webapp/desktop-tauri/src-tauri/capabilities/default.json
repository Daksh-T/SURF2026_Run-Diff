{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Run·Diff shell window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
